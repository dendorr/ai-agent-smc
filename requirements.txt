# AI Agent SMC - Python dependencies
# Install with:
#   python -m pip install -r requirements.txt

# Configuration: loads <repo>/.env in config/config.py
python-dotenv

# API server
fastapi
uvicorn[standard]
python-multipart
pydantic

# LLM client - OpenAI-compatible APIs: Ollama, vLLM, SGLang
openai

# Vector database
chromadb
# Required by chromadb OllamaEmbeddingFunction (scripts/embeddings.py)
ollama

# Excel, CSV and tables
openpyxl
pandas
tabulate
xlrd

# PDF processing
PyMuPDF
pdfplumber
markitdown

# PowerPoint and Word
python-pptx
python-docx

# OCR
# System packages required:
#   tesseract-ocr
#   tesseract-ocr-ita
#   tesseract-ocr-eng
pytesseract
easyocr
Pillow

# CAD and technical files
ezdxf
lxml
numpy-stl
# STL parsing in drawings_agent (numpy-stl is the fallback)
trimesh
# IFC/BIM parsing in drawings_agent
ifcopenshell
# DXF preview rendering via ezdxf.addons.drawing.matplotlib
matplotlib

# File watching
watchdog

# Embeddings
sentence-transformers
onnxruntime

# Numeric processing
numpy